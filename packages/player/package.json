{
  "name": "@polyptych/player",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Per-screen renderer — connects over WS, renders its screen's slice, applies changes via DOM diff (no reload).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@polyptych/protocol": "workspace:*",
    "solid-js": "^1.8.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vite-plugin-solid": "^2.10.0"
  }
}
